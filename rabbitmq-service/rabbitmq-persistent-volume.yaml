apiVersion: v1
kind: PersistentVolume
metadata:
  name: rabbitmq-pv
  namespace: quote-of-the-day-app
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/rabbitmq"
